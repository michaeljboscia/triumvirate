# Pantheon handoff, 2026-08-26

**Written at:** end of the overnight corpus-remediation run plus the first morning of actual execution.
**Read this first, then `gcp-test-plan/REVIEW-PROGRESS.md` for the detailed queue state.**
**Repo state:** branch `main`, synced with origin, 48 commits today, nothing uncommitted except `.goatrodeo.events.log`.

---

## 1. What this session did, in one paragraph

The Pantheon corpus (roughly 250KB of test plans, gate runbooks, and hardware strategy, all written between April and
August 2026) had never been reviewed and had never been executed. This session reviewed every document section by
section with three peers through Triumvirate (Codex, Gemini, DeepSeek), rewrote what survived, archived what did not,
and then, this morning, **ran the first commands the corpus has ever produced**. Execution immediately falsified three
claims in documents written hours earlier. That is the headline: the review was worth doing, and it was still no
substitute for running one command.

---

## 2. State of the goal

The `/goal` that was actually set (`GOAL-corpus-remediation.md`: review, rewrite and commit every queue item) is
**complete**: 9 of 9 items, 281 logged findings, 26 verbatim peer records in `gcp-test-plan/review-raw/`, all pushed.
**Do not re-run the queue.**

The goal file's own `DONE WHEN` list is **5 of 6**:

| # | Condition | Status |
|---|---|---|
| 1 | Every queue file has peer review plus rewrite committed | MET |
| 2 | `gcloud projects describe pantheon-validation-v1` succeeds | **MET (this morning)** |
| 3 | 10-PREFLIGHT Part 1 run start to finish | **MET (this morning, 3/3 on lenovo)** |
| 4 | No document asserts a capability with no implementation | **PARTIAL, by design** |
| 5 | `HARDWARE_DECISION.md` no longer `Status: ACCEPTED` | MET |
| 6 | Every CRITICAL hit-list item closed with its commit | MET |

Condition 4 is partial deliberately, under the owner's standing rule: **nothing unimplemented was deleted.** It is
marked `NOT BUILT` with what it was for, whether the problem persists, and what would replace it. The clearest live
example is the evidence-independence mechanism in `20-EVIDENCE-BUNDLE-SPEC.md`.

---

## 3. What physically exists now (this is new, nothing here existed yesterday)

**GCP project `pantheon-validation-v1`**
- Project number `598523033903`, `ACTIVE`, billing linked to `01F713-7EFFD2-83E164`.
- APIs enabled: compute, artifactregistry, storage, cloudbuild, logging, monitoring, pubsub, billingbudgets,
  cloudfunctions, run, eventarc, iam.
- **No VM, no GPU, no disk. Spend to date: zero.**
- Budget `effb4df0-52cb-47e2-98b4-f8a0875a7fc9`, $100, thresholds 50/90/100%. **It is an alert, not a cap.**

**Verified working on `lenovo` (192.168.2.112)**
- RTX 4000 Ada, 12282 MiB, driver 595.84, compute 8.9, headless.
- Docker present; **GPU-in-container path works** (the check the preflight called most likely to fail).

---

## 4. The four findings from execution, and why they matter

Full record with verbatim command output: **`gcp-test-plan/runs/2026-08-26-first-execution.md`**.

**R-1 (retracted and corrected twice).** The 2598 MiB resident on the lenovo GPU is **not** a hardware limit and
**not** a stuck job. It is the `infinity` container serving `BAAI/bge-reranker-v2-m3`, which is **Graphiti's
cross-encoder reranker**. I first reported it as a desktop session (wrong, the box is headless), then stopped it
believing it unused (wrong, see §5). Usable VRAM is ~11.6GB clear, ~9.3GB with the reranker up.

**R-2.** The local `gcloud` has a quota project pinned to `e5bravo-workspace-cli`, unrelated to Pantheon. Billing,
quota and serviceusage calls fail with `USER_PROJECT_DENIED` naming that project, which is actively misleading. **Pass
`--billing-project` explicitly on every such command.**

**R-3.** A GCP budget is an alert, not a cap. I first named it "hard cap", which is false, and renamed it. Recorded
rather than quietly fixed, because it is the corpus's signature defect: naming a control after the outcome you want
instead of the behavior it has.

**R-4, and this one changes priorities.** The rewritten doc says new projects start at **zero** GPU quota. **False on
this account.** Minutes after creation: **L4 16, A100 40GB 16, T4 8, V100 8** in us-central1. Only A100 80GB is zero.
So there is **no zero-quota backstop against accidental GPU spend**. Combined with R-3, the current posture is sixteen
launchable L4s behind a budget that only sends email.

> **The global `GPUs (all regions)` quota was NOT verified.** It is no longer exposed by
> `gcloud compute project-info describe`, and the Cloud Quotas API needs the `alpha` component, which is not
> installed. Treat as unknown in both directions, not as cleared.

---

## 5. The reranker incident, and the rule it produced

I stopped the `infinity` container to free VRAM after concluding it had no consumers. **It had one.** Graphiti
(`docker-graphiti-mcp-1`) on **homebox (192.168.2.110)** reaches it at `LOCAL_RERANK_URL=http://192.168.2.112:7997`.

Outage was about ten minutes. **Impact was zero**, because Graphiti's `fallback_reranker` caught the dead endpoint,
logged a WARNING, fell down to tier 1 (`gemini-2.5-flash-lite`), and served both affected calls. Restored and verified
end to end from homebox: `HTTP_STATUS=200 TIME=0.036s`. ContentFactory was never a consumer.

**Why the consumer check failed**, because the method error is reusable:
1. The dependency is a **container environment variable**, not source code. No source grep can see `docker inspect` env.
2. The consumer is on a **third machine**. I searched the Mac and lenovo; it was on neither.
3. `queue_absolute: 0` read as "abandoned" when it only means "not busy this second."

The service was naming its own caller 3,520 times in output I had already pulled:

```bash
docker logs infinity | grep -oE '"(GET|POST) [^ ]+ HTTP' | sort | uniq -c | sort -rn
#   3520 "POST /rerank HTTP
docker logs infinity | grep -oE '^INFO: +[0-9.]+:' | sort -u
# INFO:     192.168.2.110:
```

> **RULE: enumerate a service's callers from its own access log before stopping it, never from a source grep.**
> A source grep answers "who was written to call this," which is blind to env vars, containers, and other hosts.

Captured in the HNT inbox and in memory `lenovo-gpu-shared-with-graphiti-reranker`.

---

## 6. Operating constraints (learned the hard way, do not relearn)

- **DeepSeek has no filesystem access** through the bridge. It correctly refuses to fabricate citations. Give it
  method-level questions or paste content.
- **The MCP bridge times out at 180s** (`TRIUMVIRATE_DAEMON_ASK_TIMEOUT_SECS`, read client-side in
  `daemon-http/src/lib.rs:437`).
- **There are ~3 twin worker slots.** A 10-agent fan-out with 3 twin calls each deadlocks. This happened, cost roughly
  500K tokens, and produced nothing. **Do not use the Workflow tool for peer review here.** Three direct `ask_agent`
  calls per section, one section at a time. That is the instruction that worked.
- **`dispatch_codex` / `dispatch_codex_worktree` are for Codex writing code in a repo only.** Never for analysis or
  document review.

---

## 7. What to do next, in priority order

**1. Build a real spend stop before the first GPU VM.** R-3 and R-4 compound: launchable GPUs plus a
notification-only budget equals no enforced ceiling. A budget becomes a cap only when its Pub/Sub notification drives
a function that disables billing. **That function does not exist.** Today the only real controls are
`--max-run-duration` on instances and the teardown trap in `gate-0-plumbing.md`. This is now the highest-priority
build, and R-4 is why it comes *before* the sweep rather than after.

**2. Run Track A on lenovo.** Gate 0 (plumbing) then Gate 6 (restricted-egress / isolation). Both runbooks were
rewritten and neither has ever been executed. Budget ~9.3GB VRAM with the reranker up, or stop it deliberately and
restore it after:
```bash
ssh lenovo 'docker stop infinity'    # frees 2.6GB, stays down across reboots
ssh lenovo 'docker start infinity'   # ~8s to reload weights
```

**3. Author the fixtures.** `gcp-test-plan/fixtures/{agent-tasks,eval-scorers,embed-corpus}` are all `NOT WRITTEN`.
Preflight Rule 1 says no gate may be committed until the fixture validation gate passes. This is evaluation-methodology
design, not an upload; the original's "1-2 hours" estimate was for the wrong activity.

**4. Refresh the model list.** `10-PREFLIGHT.md` §1.3 is April 2026 vintage and says so. Verify current small,
coding, embedding and reasoning models before use rather than trusting those names. Same for the vLLM pin: `v0.6.5` is
a late-2024 release predating roughly two years of FlashAttention, batching and FP8 work, and
`vllm/vllm-openai:v0.6.5-cpu` **does not exist** (404).

**5. Fill the NOT SET thresholds in `POLICY-rent-first.md`.** DeepSeek's argument stands: a pre-commitment without a
named reopening condition is evidence insulation, not discipline. The numbers must come from measured spend and
utilization, so this waits on Track B.

**6. The five unrecovered findings.** Caught by the April twin review, never fixed, and **missed again by this
review**: human-in-the-loop operating protocol; continuous model evaluation; source-of-truth under concurrent edits;
**customer discovery as a gate before building**; client-codebase security across its lifecycle. Carried in
`twin-review-synthesis.md`. The fourth is uncomfortable in context: the corpus was rebuilt over one night without a
customer.

**7. Optional but cheap.** Alert on sustained tier-0 absence in Graphiti's reranker. The fallback converts loud
failures into quiet ones, which is right for uptime and wrong for detection; without an alert, running on the paid
fallback tier for weeks is invisible.

---

## 8. Map of the corpus

| Path | What it is |
|---|---|
| `GOAL-corpus-remediation.md` | The goal file. Queue is COMPLETE, do not re-run. |
| `gcp-test-plan/REVIEW-PROGRESS.md` | **Authoritative state.** Queue table, 281 findings, RESUME HERE pointer. |
| `gcp-test-plan/runs/2026-08-26-first-execution.md` | **The only record of anything actually executed.** |
| `gcp-test-plan/REVIEW-HIT-LIST.md` | Original hit list, all 5 CRITICAL items closed with commits. |
| `gcp-test-plan/review-raw/*.md` | 26 verbatim peer records. Never edit, these are evidence. |
| `gcp-test-plan/00-MASTER-PLAN.md`, `10-PREFLIGHT.md`, `20-EVIDENCE-BUNDLE-SPEC.md`, `30-DECISION-RULES.md` | Rewritten. |
| `gcp-test-plan/SIZING-SWEEP-METHOD.md` | Track B. Replaces six demoted runbooks. |
| `gcp-test-plan/runbooks/gate-0-plumbing.md`, `gate-6-airgap-sanity.md` | Track A. Rewritten, never run. |
| `POLICY-rent-first.md` | The standing policy, extracted. Thresholds NOT SET. |
| `local-inference-buy-vs-rent.md` | DEMOTED, banner lists its defects. |
| `EXTRACTED-from-archive.md` | What survived the archived documents. |
| `fast-vm-startup-strategies.md` | Cold-start strategies with precomputed costs. |
| `archive/`, `gcp-test-plan/runbooks/archive/` | 10 archived docs. Nothing was deleted. |

---

## 9. Standing rules from the owner

- **Do not delete and hand-wave.** For anything cut, state what it was for, whether the problem persists, and what
  replaces it.
- **Durable artifacts on disk** at the end of every peer run, and a durable checkpoint at every synthesis.
- **Air-gap does not have to be literal.** It means not connected to the public internet. PGA rides Google's private
  backbone and does not need removing; the defect there was terminology, not architecture.
- **No em dashes. No timeline estimates.** Both are hard walls.
- **Rent first, always.** Owned metal only as a customer-funded terminal step after a rented pilot and a signed term.
