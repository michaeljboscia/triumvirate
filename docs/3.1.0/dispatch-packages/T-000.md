# Dispatch Package — T-000

**Task:** Preflight — align Cargo workspace version to 3.1.0 and wire version reporting
**Wave:** -1 (Preflight) — encoded as `wave: 0` in ContractFields (uint32 cannot go negative)
**REQs:** PREFLIGHT
**Baseline SHA:** `90974f4db7ae6291af87be3030faee78da304dcc`
**Orchestrator:** Claude (not an auditor, not an executor)
**Round:** 3 (R1 Codex REJECTED 4 high + 1 medium → R2 fixed; R2 Codex REJECTED 1 high for canonical-doc conflict + 1 low nav hint → R3 fixes)

---

## 0. Delta log

### R1 → R2 (fixes from R1 Codex audit)
1. **Briefing step 4(c) rewritten.** R1 incorrectly said "Update HealthResponse struct" — that struct does not exist in the source. The `/health` route handler at `main.rs:1700` returns `AxumJson<serde_json::Value>` built via an inline `serde_json::json!({...})` literal. R2 instructs the worker to add a `"version"` field directly to that JSON literal.
2. **cli_ops.rs added to allowed_files.** R1 omitted `daemon/crates/triumvirate/src/cli_ops.rs` — but the spec's `reality_test` requires `triumvirate doctor` to print `3.1.0`, and `run_doctor()` lives at `cli_ops.rs:59`. R2 adds the file and a new briefing step (step 5) that edits `run_doctor()` to emit a version line.
3. **Spec XML `<files>` updated** to include `cli_ops.rs`. This was a Phase 4.4 miss that Phase 5.3 caught — logged in DISPATCH_AUDIT.md for Phase 5.6 reconciliation.
4. **test_command upgraded** from `cargo build --release` (compile-only) to a composite shell gate that verifies version-string reach, script executability, and ROADMAP presence.
5. **reality_test strengthened** to include doctor-output assertion (#6) and no-hardcoded-literal scan (#8).

### R2 → R3 (fixes from R2 Codex audit)
6. **Canonical doc sync.** R2 audit found that `IMPLEMENTATION_PLAN.md` "T-000 Implementation Details" section 4 still described the phantom `HealthResponse` struct approach — contradicting the R2 briefing. R3 rewrites that section to the inline `serde_json::json!` literal and adds a new section "5b. Wire version into `triumvirate doctor`" for the cli_ops.rs edit. This was another Phase 4.4 miss that Phase 5.3 caught.
7. **Low-severity nav hint fixed.** Briefing now says `grep for struct Cli` instead of the non-existent `impl Cli`.

---

## 1. Task XML Block (after R2 fix — verbatim from IMPLEMENTATION_PLAN.md)

```xml
<task id="T-000" req="PREFLIGHT" wave="-1" depends="">
  <description>Align Cargo workspace version to 3.1.0 and wire version reporting through the codebase</description>
  <files>daemon/Cargo.toml, daemon/crates/daemon-core/src/lib.rs, daemon/crates/daemon-core/src/version.rs, daemon/crates/triumvirate/src/main.rs, daemon/crates/triumvirate/src/cli_ops.rs, scripts/install-git-hooks.sh, scripts/version-drift-check.sh, ROADMAP.md</files>
  <scope_out>Do not bump individual crate-level version fields (they inherit via version.workspace = true). Do not tag git yet. Do not touch dashboard/package.json or mcp-server/package.json. Do not add a changelog file. Do NOT place hook scripts under .git/ (untrackable) — place them in scripts/ and install via scripts/install-git-hooks.sh.</scope_out>
  <tools>cargo check --workspace --manifest-path daemon/Cargo.toml, cargo build --release --manifest-path daemon/Cargo.toml, grep</tools>
  <verify>cargo build --release --manifest-path daemon/Cargo.toml</verify>
  <reality_test>After the change, all of these must be true: (1) grep '^version = "3.1.0"' daemon/Cargo.toml returns a match; (2) cargo build --release --manifest-path daemon/Cargo.toml exits 0; (3) ./daemon/target/release/triumvirate --version prints a string containing 3.1.0; (4) ./daemon/target/release/triumvirate doctor prints a string containing 3.1.0 (or add version to doctor output); (5) scripts/version-drift-check.sh exists and is executable; (6) scripts/install-git-hooks.sh exists and, when run, installs a pre-commit hook that rejects a test file declaring a mismatched version.</reality_test>
  <done_when>Cargo workspace at 3.1.0. Rust code reads VERSION from daemon_core::version via env!("CARGO_PKG_VERSION") — zero hardcoded version strings. MCP get_info() instructions include 3.1.0. CLI --version prints 3.1.0. scripts/version-drift-check.sh committed to repo (trackable). scripts/install-git-hooks.sh committed and documented. ROADMAP.md updated: "Current version: 3.1.0 (in progress)".</done_when>
</task>
```

Implementation details referenced by the spec live at `docs/3.1.0/IMPLEMENTATION_PLAN.md` lines 70-206 (section "T-000 Implementation Details"). The worker MUST read that section before coding — it contains the exact Cargo.toml snippet, the version.rs source, the hook scripts, and the ROADMAP.md patch.

---

## 2. Proposed Briefing (plain English for the worker)

```
You are an ABE worker executing task T-000 for Triumvirate v3.1.0.

GOAL: Bump the Cargo workspace version from 0.1.0 to 3.1.0 and plumb
the version string through daemon-core, the MCP get_info() response,
the HTTP /health route's inline JSON literal, the CLI --version flag,
and the `triumvirate doctor` output. Add a tracked version-drift check
script plus an installer. Update ROADMAP.md.

INPUTS TO READ FIRST (in order):
1. docs/3.1.0/IMPLEMENTATION_PLAN.md — section "T-000 Implementation
   Details" at line 70. Contains the exact Cargo.toml edit, the
   version.rs file contents, the hook scripts, and the ROADMAP patch.
   Do NOT improvise values — copy from the spec verbatim where shown.
2. daemon/Cargo.toml — confirm current [workspace.package] version is
   the stale string before editing.
3. daemon/crates/daemon-core/src/lib.rs — confirm existing module
   layout so `pub mod version; pub use version::{VERSION, NAME};`
   slots in cleanly.
4. daemon/crates/triumvirate/src/main.rs — grep for `get_info`,
   `async fn health`, `struct Cli`, and `#[command(` to locate the
   edit points. Do NOT trust the line-number hints in this briefing —
   grep is authoritative.
5. daemon/crates/triumvirate/src/cli_ops.rs — grep for `run_doctor`
   (around line 59). Confirm the existing `write_line_stdout("Daemon:")`
   block where the version line will be inserted.

STEPS:

1. Edit daemon/Cargo.toml: set [workspace.package] version = "3.1.0".
   Do NOT touch any per-crate version field — all 12 crates inherit
   via `version.workspace = true`.

2. Create daemon/crates/daemon-core/src/version.rs with:
     pub const VERSION: &str = env!("CARGO_PKG_VERSION");
     pub const NAME: &str = env!("CARGO_PKG_NAME");

3. Edit daemon/crates/daemon-core/src/lib.rs: add
     pub mod version;
     pub use version::{VERSION, NAME};

4. Edit daemon/crates/triumvirate/src/main.rs:
   (a) In the McpBridge ServerHandler get_info() impl, format the
       instructions string with daemon_core::VERSION — do NOT inline
       the literal "3.1.0" anywhere.
   (b) If the Cli clap struct lacks #[command(version)], add it.
       Clap auto-reads CARGO_PKG_VERSION at compile time.
   (c) In the `async fn health(...)` handler, the response is an
       inline `serde_json::json!({...})` literal. Add a new field
       `"version": daemon_core::VERSION` to that literal. Do NOT
       introduce a typed `HealthResponse` struct — there is no such
       struct today; keeping the inline literal avoids invented scope.

5. Edit daemon/crates/triumvirate/src/cli_ops.rs:
   In `run_doctor()` (around line 59), insert ONE line as the very
   first stdout write — before the existing `write_line_stdout("Daemon:")`
   call. Use:

     write_line_stdout(&format!("Triumvirate daemon v{}", daemon_core::VERSION))?;

   This is the minimum change that satisfies reality_test #6. Do NOT
   restructure any other doctor output.

6. Create scripts/version-drift-check.sh (mode 755). Content verbatim
   from IMPLEMENTATION_PLAN.md lines 140-170.

7. Create scripts/install-git-hooks.sh (mode 755). Content verbatim
   from IMPLEMENTATION_PLAN.md lines 174-192.

8. Update ROADMAP.md header:
   **Current version:** 3.1.0 (in progress — MCP Consolidation sprint)
   Add a shipped section for 3.0.0 if one does not already exist.

9. Run `cargo check --workspace --manifest-path daemon/Cargo.toml` — must pass clean on edited files.

10. Run `cargo build --release --manifest-path daemon/Cargo.toml` — must produce
    daemon/target/release/triumvirate.

11. Commit with message EXACTLY:
    chore(3.1.0): bump workspace version, wire version reporting, add drift hook script

CONSTRAINTS (hard stops):
- Do NOT modify any per-crate Cargo.toml file. They inherit.
- Do NOT introduce a HealthResponse struct. The /health route stays
  on serde_json::Value — just add a "version" field to the json! literal.
- Do NOT touch mcp-server/, dashboard/, or any TS file. TS side is
  parked until Wave 4 archival.
- Do NOT add a CHANGELOG file. That lands in Wave 5 (T-021).
- Do NOT tag the git commit (no `git tag`). Tagging happens in Wave 5.
- Do NOT install the pre-commit hook into .git/hooks/ yourself.
  Only COMMIT the scripts/ files. The orchestrator runs
  scripts/install-git-hooks.sh after the task lands.
- Do NOT introduce a hardcoded version string "3.1.0" anywhere in
  Rust source. All version reads go through daemon_core::VERSION.
  The only place "3.1.0" appears as a literal is Cargo.toml itself
  (and ROADMAP.md prose).
- Do NOT update docs/3.1.0/*.md — those were audited in Phase 4.4.

HOW YOU'LL KNOW YOU'RE DONE:
- `grep '^version = "3.1.0"' daemon/Cargo.toml` prints one line
- `cargo build --release --manifest-path daemon/Cargo.toml` exits 0
- `./daemon/target/release/triumvirate --version` contains "3.1.0"
- `./daemon/target/release/triumvirate doctor 2>&1 | head -3` contains "3.1.0"
- `test -x scripts/version-drift-check.sh` exits 0
- `test -x scripts/install-git-hooks.sh` exits 0
- ROADMAP.md's header lists 3.1.0 as current
- Exactly one new commit exists, matching commit_format
```

---

## 3. Proposed ContractFields (JSON)

```json
{
  "task_id": "T-000",
  "req_ids": ["PREFLIGHT"],
  "wave": 0,
  "file_policy": "default-deny",
  "allowed_files": [
    "daemon/Cargo.toml",
    "daemon/Cargo.lock",
    "daemon/crates/daemon-core/src/lib.rs",
    "daemon/crates/daemon-core/src/version.rs",
    "daemon/crates/triumvirate/src/main.rs",
    "daemon/crates/triumvirate/src/cli_ops.rs",
    "scripts/version-drift-check.sh",
    "scripts/install-git-hooks.sh",
    "ROADMAP.md"
  ],
  "forbidden_files": [
    "daemon/crates/*/Cargo.toml",
    "mcp-server/**",
    "dashboard/**",
    "docs/**",
    "specs/**",
    ".github/**",
    ".git/**",
    "CHANGELOG.md"
  ],
  "allowed_commands": [
    ["bash", "-c"],
    ["sh", "-c"],
    ["cargo", "check", "--workspace", "--manifest-path", "daemon/Cargo.toml"],
    ["cargo", "build", "--release", "--manifest-path", "daemon/Cargo.toml"],
    ["cargo", "fmt", "--all", "--manifest-path", "daemon/Cargo.toml"],
    ["./daemon/target/release/triumvirate", "--version"],
    ["./daemon/target/release/triumvirate", "doctor"],
    ["grep", "-rn"],
    ["grep", "-n"],
    ["grep", "-c"],
    ["grep", "-E"],
    ["grep", "-q"],
    ["git", "add"],
    ["git", "commit"],
    ["git", "status"],
    ["git", "diff"],
    ["git", "rev-parse"],
    ["chmod", "+x"],
    ["ls", "-la"],
    ["test", "-x"],
    ["test", "-f"]
  ],
  "forbidden_commands": [
    ["git", "push"],
    ["git", "tag"],
    ["git", "reset"],
    ["git", "rebase"],
    ["rm", "-rf"],
    ["cargo", "install"],
    ["cargo", "publish"],
    ["cargo", "update"],
    ["npm"],
    ["pnpm"]
  ],
  "commit_format": "^chore\\(3\\.1\\.0\\): bump workspace version, wire version reporting, add drift hook script$",
  "test_command": "bash -c 'cargo build --release --manifest-path daemon/Cargo.toml && grep -q \"^version = \\\"3.1.0\\\"\" daemon/Cargo.toml && ./daemon/target/release/triumvirate --version 2>&1 | grep -q 3.1.0 && ./daemon/target/release/triumvirate doctor 2>&1 | grep -q 3.1.0 && test -x scripts/version-drift-check.sh && test -x scripts/install-git-hooks.sh && grep -q 3.1.0 ROADMAP.md && ! grep -rn \"\\\"3.1.0\\\"\" daemon/crates/triumvirate/src/ daemon/crates/daemon-core/src/'",
  "task_timeout_sec": 1800,
  "done_when": "Cargo workspace at 3.1.0. Rust code reads VERSION from daemon_core::version via env!(\"CARGO_PKG_VERSION\") with zero hardcoded 3.1.0 literals in Rust source. MCP get_info() instructions include 3.1.0 via daemon_core::VERSION. CLI --version prints 3.1.0. HTTP /health json! literal includes a version field. `triumvirate doctor` output includes a version line. scripts/version-drift-check.sh and scripts/install-git-hooks.sh are committed, executable, and referenced correctly. ROADMAP.md updated to list 3.1.0 as current. Exactly one new commit landed matching commit_format.",
  "reality_test": "All of these must be true after the commit: (1) `grep '^version = \"3.1.0\"' daemon/Cargo.toml` prints one match; (2) `cargo build --release --manifest-path daemon/Cargo.toml` exits 0; (3) `./daemon/target/release/triumvirate --version 2>&1 | grep -q 3.1.0` exits 0; (4) `test -x scripts/version-drift-check.sh` exits 0; (5) `test -x scripts/install-git-hooks.sh` exits 0; (6) `./daemon/target/release/triumvirate doctor 2>&1 | grep -q 3.1.0` exits 0 (proves cli_ops::run_doctor was updated); (7) `grep -q 'daemon_core::VERSION' daemon/crates/triumvirate/src/main.rs` exits 0 (proves the MCP get_info plumbs through, not hardcoded); (8) `! grep -rn '\"3.1.0\"' daemon/crates/triumvirate/src/*.rs daemon/crates/daemon-core/src/*.rs` — no hardcoded 3.1.0 literals in Rust source (env! macro reads from Cargo.toml); (9) `grep -q '3.1.0' ROADMAP.md` exits 0."
}
```

---

## 4. Relevant BACKEND_STRUCTURE sections

T-000 is preflight — it touches version plumbing, not the target architecture. No module shape changes. BACKEND_STRUCTURE.md's crate naming clarification (`mcp-bridge` vs `mcp-tools`) is not touched. The only architectural fact T-000 establishes: **`daemon-core` becomes the single source of truth for the version string**, which all downstream modules in Wave 0+ will read via `daemon_core::VERSION`.

---

## 5. Dependency Context

**Prior committed tasks this depends on:**

- `FIX-TEST-MOVED-VALUES` — committed at `be545585` on 2026-04-09. Unblocks `cargo test -p triumvirate abe_red_team_enforcement_blocks_non_compliant_worker`. T-000 itself does not run the test suite (only build), so this is context rather than a hard prerequisite, but future waves regress-test against this fix.
- `90974f4db7ae6291af87be3030faee78da304dcc` — doc audit Round 5 APPROVED. This is the baseline worktree SHA. All T-000 work happens on a worktree branched from this commit.

**No prior T-### tasks have executed.** T-000 is the first dispatched task of the sprint.

---

## 6. Audit Ask

Apply the Phase 5.3 audit checklist from `~/.claude/skills/goatrodeo.md` lines 1011-1029. Return the JSON verdict shape defined at lines 1030-1046. REJECTED on any critical or high finding. Read referenced files directly before speculating — the triumvirate repo lives at `/Users/mikeboscia/projects/triumvirate`.
