# Dispatch Package — T-002

**Task:** Wave 0 — Define alias parameter mapping types and TS→Rust schema conversion functions
**Wave:** 0 (Contracts and Interfaces)
**REQs:** REQ-A1, REQ-A2
**Baseline SHA:** `882a1134537010c98d4505c9ad7ef3609bb8af4b` (main HEAD after T-000)
**Orchestrator:** Claude (not an auditor, not an executor)
**Round:** 2 (R1 REJECTED: Gemini 3 high — XML↔contract parity, thiserror NOT in workspace deps, write_scratchpad cwd contradictory between "required" in briefing and "None" in test; Codex 1 high + 1 medium — same parity issue plus ambiguous serde/thiserror optional language. R2 fixes all five by: tightening scope to forbid Cargo.toml changes entirely, mandating plain struct literals + manual Display impl, fixing write_scratchpad cwd as Option, and dropping mcp-tools/Cargo.toml from allowed_files.)

---

## 0. Pre-audit empirical verification (Rule B)

1. `cargo check -p mcp-tools --manifest-path daemon/Cargo.toml` → exits 0 (baseline clean). Note: without `--manifest-path`, this fails from project root (subdirectory workspace). Every cargo command in this task uses the flag.
2. `ls daemon/crates/mcp-tools/src/` → only `lib.rs` exists. `aliases.rs` does NOT exist (correct — T-002 creates it).
3. Read `daemon/crates/mcp-tools/src/lib.rs` → existing contents: `ProgressEmitter` struct + impl, `display_agent_name()`, `next_heartbeat_offset()`. Uses `rmcp`, `serde_json`, `tokio`. Will gain `pub mod aliases;` at the top.
4. Read `daemon/crates/mcp-tools/Cargo.toml` → deps: rmcp, serde_json, tokio, tracing. Does NOT include `serde` or `thiserror` or `anyhow` directly. Worker will need to decide whether to add `anyhow.workspace = true` or `thiserror.workspace = true` (both are in workspace.dependencies) for the custom error type, or inline an enum error.
5. Read `specs/MCP_CONSOLIDATION.md:60-74` (TS tool table with param shapes) and `:176-195` (alias matrix with full parameter mappings) → canonical source for all 10 aliases.
6. Confirmed all 10 alias names from the canonical alias matrix: `spawn_daemon`, `ask_daemon`, `dismiss_daemon`, `list_daemons`, `send_message`, `get_response`, `list_jobs`, `write_scratchpad`, `list_scratchpad`, `code_review`.

No new blocking findings. The original spec's `cargo check -p mcp-tools` (without manifest path) bug was already fixed in the T-002 XML rewrite earlier this session.

---

## 1. Task XML Block (verbatim from IMPLEMENTATION_PLAN.md after 2026-04-09 fixes)

```xml
<task id="T-002" req="REQ-A1,REQ-A2" wave="0" depends="">
  <description>Define alias parameter mapping types and the TS→Rust schema conversion functions</description>
  <files>daemon/crates/mcp-tools/src/aliases.rs, daemon/crates/mcp-tools/src/lib.rs, .triumvirate/TASK_COMPLETE.json</files>
  <scope_out>Do not register aliases with tool_router yet. Do not modify McpBridge. Types and mapping functions only. Do not touch main.rs or any other crate. Do NOT modify mcp-tools/Cargo.toml or any Cargo.toml file — the worker MUST use plain struct literals without serde derive macros and MUST use a manual `impl std::fmt::Display` for the error enum (no `thiserror` — it is NOT in the workspace dependencies).</scope_out>
  <tools>cargo check -p mcp-tools --manifest-path daemon/Cargo.toml, cargo test -p mcp-tools --manifest-path daemon/Cargo.toml</tools>
  <verify>cargo check -p mcp-tools --manifest-path daemon/Cargo.toml</verify>
  <reality_test>Unit tests in aliases.rs: (1) Call map_spawn_daemon_params with TS schema { target: "gemini", session_name: "x" } → asserts returns Rust schema { agent: "gemini", name: "x" }. (2) Call with { target: "codex" } → asserts returns { agent: "codex" }. (3) Call with { target: "claude" } → asserts returns error (strict enum — only "gemini" and "codex" allowed). (4) Call map_ask_daemon_params with { daemon_id: "gd_session_abc", question: "hi" } → asserts returns { name: "gd_session_abc", message: "hi" } (preserves prefix). (5) `cargo test -p mcp-tools --manifest-path daemon/Cargo.toml` exits 0 with all mapping tests passing. A stub returning default-empty values for all mappings cannot pass the strict-enum error case (#3).</reality_test>
  <done_when>daemon/crates/mcp-tools/src/aliases.rs created with all 10 alias mapping functions defined and unit-tested: spawn_daemon, ask_daemon, dismiss_daemon, list_daemons, send_message, get_response, list_jobs, write_scratchpad, list_scratchpad, code_review. mcp-tools/src/lib.rs re-exports the module (`pub mod aliases;`). Parameter conversion matches the canonical alias matrix in specs/MCP_CONSOLIDATION.md REQ-A1. `cargo check -p mcp-tools --manifest-path daemon/Cargo.toml` exits 0. Unit tests exist for at least: a valid mapping, an invalid enum value (error path), and the daemon_id prefix-preservation case.</done_when>
</task>
```

---

## 2. Proposed Briefing (plain English for the worker)

```
You are an ABE worker executing task T-002 for Triumvirate v3.1.0.

GOAL: Create a new Rust module at
`daemon/crates/mcp-tools/src/aliases.rs` containing parameter-mapping
types and conversion functions for all 10 TS→Rust tool aliases. This
is a Wave 0 contract-definition task — the types and functions exist,
but they are NOT yet wired into the tool_router or McpBridge. Wiring
happens later in Wave 3 T-011.

INPUTS TO READ FIRST (in order):

1. specs/MCP_CONSOLIDATION.md lines 60-74 (TS tool table) for each
   alias's INPUT parameter shape (the TS schema)
2. specs/MCP_CONSOLIDATION.md lines 176-195 (alias matrix) for the
   canonical TS→Rust mapping rules — this is the SOURCE OF TRUTH
3. docs/3.1.0/BACKEND_STRUCTURE.md lines 146-167 (aliases.rs section)
   for the mapping table
4. daemon/crates/mcp-tools/src/lib.rs — current contents; you'll add
   `pub mod aliases;` at the top
5. daemon/crates/mcp-tools/Cargo.toml — current deps (rmcp,
   serde_json, tokio, tracing). You may need to add `serde.workspace = true`
   if you use `#[derive(Deserialize)]` on the input param structs,
   and/or `thiserror.workspace = true` for a clean error type. Both
   exist in [workspace.dependencies].

STEPS:

1. Add `pub mod aliases;` to daemon/crates/mcp-tools/src/lib.rs. Place
   it near the top of the file, after the existing `use` statements
   and before the ProgressEmitter struct. Keep everything else in
   lib.rs unchanged.

2. Do NOT modify mcp-tools/Cargo.toml. The struct literals + manual
   Display impl pattern used below does NOT require any new
   dependencies. `serde` derive macros are NOT needed because you will
   use plain struct literals in unit tests (no JSON deserialization at
   this module level — that happens later when tool_router registers
   the aliases in Wave 3 T-011). `thiserror` is NOT available — it is
   NOT in the workspace dependencies. Use the manual `impl std::fmt::Display`
   shown in step 3(b) for the error enum.

3. Create daemon/crates/mcp-tools/src/aliases.rs with the following
   structure:

   (a) Module-level docstring explaining that this module contains
       backwards-compatibility aliases for the TS inter-agent tool
       names, and that actual tool_router registration happens in
       Wave 3 T-011.

   (b) An error type for invalid parameter values:

       #[derive(Debug, Clone, PartialEq, Eq)]
       pub enum AliasMappingError {
           InvalidTarget(String),
           MissingRequired(&'static str),
           InvalidDaemonId(String),
       }

       impl std::fmt::Display for AliasMappingError {
           fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
               match self {
                   Self::InvalidTarget(t) => write!(f, "invalid target '{t}' — expected 'gemini' or 'codex'"),
                   Self::MissingRequired(field) => write!(f, "missing required field '{field}'"),
                   Self::InvalidDaemonId(id) => write!(f, "invalid daemon_id '{id}' — expected 'gd_*' or 'cd_*' prefix"),
               }
           }
       }

       impl std::error::Error for AliasMappingError {}

   (c) TS-side input param structs (one per alias). These are the
       shapes the TS client sends. Every field corresponds to a TS
       tool parameter from specs/MCP_CONSOLIDATION.md lines 60-74.
       Example for spawn_daemon:

       #[derive(Debug, Clone)]
       pub struct SpawnDaemonParams {
           pub target: String,           // "gemini" | "codex"
           pub session_name: Option<String>,
           pub cwd: Option<String>,
           pub timeout_ms: Option<u64>,
       }

   (d) Rust-side output param structs. For most aliases, these
       correspond to existing Rust tool schemas (SpawnSessionRequest,
       AskSessionRequest, etc.). Define them LOCALLY in aliases.rs for
       now — Wave 3 T-011 will replace them with the real types when
       tool_router registration happens. Keeping them local here
       avoids cross-crate dependency complexity in Wave 0.

       Example:

       #[derive(Debug, Clone, PartialEq, Eq)]
       pub struct SpawnSessionRequestLike {
           pub agent: String,
           pub name: Option<String>,
           pub cwd: Option<String>,
           pub timeout_ms: Option<u64>,
       }

       You can use the actual Rust schema types if mcp-tools already
       depends on the crate where they live — but check first. Do
       NOT add new cross-crate dependencies just to use "the real"
       types. Local shadow types are fine for Wave 0.

   (e) Mapping functions — one per alias. Each takes the TS input
       struct by value and returns Result<RustOutputLike, AliasMappingError>.
       All 10 required:

       pub fn map_spawn_daemon_params(p: SpawnDaemonParams) -> Result<SpawnSessionRequestLike, AliasMappingError>
       pub fn map_ask_daemon_params(p: AskDaemonParams) -> Result<AskSessionRequestLike, AliasMappingError>
       pub fn map_dismiss_daemon_params(p: DismissDaemonParams) -> Result<DismissSessionRequestLike, AliasMappingError>
       pub fn map_list_daemons_params(p: ListDaemonsParams) -> Result<ListSessionsRequestLike, AliasMappingError>
       pub fn map_send_message_params(p: SendMessageParams) -> Result<AskSessionRequestLike, AliasMappingError>
       pub fn map_get_response_params(p: GetResponseParams) -> Result<GetResponseDeprecationShim, AliasMappingError>
       pub fn map_list_jobs_params(p: ListJobsParams) -> Result<GetStatusRequestLike, AliasMappingError>
       pub fn map_write_scratchpad_params(p: WriteScratchpadParams) -> Result<ScratchpadWriteRequestLike, AliasMappingError>
       pub fn map_list_scratchpad_params(p: ListScratchpadParams) -> Result<ScratchpadListRequestLike, AliasMappingError>
       pub fn map_code_review_params(p: CodeReviewParams) -> Result<ReviewRequestLike, AliasMappingError>

   (f) Mapping rules — each function applies the canonical rules from
       specs/MCP_CONSOLIDATION.md lines 176-195. Key per-alias rules:

       map_spawn_daemon_params:
         - target "gemini" → agent "gemini"
         - target "codex" → agent "codex"
         - target "claude" or anything else → Err(InvalidTarget)
         - session_name → name (direct passthrough)
         - cwd, timeout_ms → direct passthrough

       map_ask_daemon_params:
         - daemon_id MUST start with "gd_" or "cd_" or return
           Err(InvalidDaemonId). Preserve prefix — DO NOT strip it.
         - daemon_id → name (preserving prefix)
         - question → message

       map_dismiss_daemon_params:
         - daemon_id → name (validate prefix as above)
         - hard param is DROPPED with a tracing::warn! log if present
           ("alias dismiss_daemon: 'hard' param dropped — Rust does
           not support it")

       map_list_daemons_params:
         - target → optional filter field (applied post-fetch by the
           future tool_router; mapping function just passes it
           through). Accept "gemini", "codex", or absent. Other values
           → Err(InvalidTarget).
         - cwd → direct passthrough

       map_send_message_params:
         - target "gemini"/"codex" → name (string literal; the
           caller auto-spawns if needed)
         - target other → Err(InvalidTarget)
         - question → message
         - context, session_log, cwd, request_type, timeout_ms →
           carry through into a free-form context field OR drop, based
           on what the Rust schema supports. For Wave 0, just define
           the output struct with the fields that matter (name,
           message) and comment-note the dropped fields.

       map_get_response_params:
         - Returns a DEPRECATION SHIM struct, not a real request.
           The shim has a single field `message: String` with value
           "get_response is deprecated in 3.1.0 — use ask_session
           directly. The async job queue was removed."
         - This function NEVER returns an error in Wave 0; it always
           returns the shim.

       map_list_jobs_params:
         - This is a SHAPE-TRANSLATED alias. Input is ListJobsParams
           with optional target/cwd. Output is GetStatusRequestLike
           with optional target filter. The shape translation of
           RESPONSE entries (job_id=session_id, state=agent_state,
           target=agent) happens in Wave 3 T-011 when the alias is
           wired up — NOT here. T-002 only maps the REQUEST shape.
         - target → optional filter (same validation as list_daemons)
         - cwd → passthrough

       map_write_scratchpad_params:
         - topic, content are required and pass through
         - cwd is Option<String> (optional — matches TS schema `{cwd?}` at
           specs/MCP_CONSOLIDATION.md line 69). Pass through as-is.
         - owner resolution priority:
             1. If daemon_id is present and starts with "gd_" → owner
                = "gemini-<suffix>" where suffix is the part after "gd_"
             2. Else if daemon_id is present and starts with "cd_" →
                owner = "codex-<suffix>"
             3. Else if explicit `owner` field is present → use it
             4. Else → default to "inter-agent"
         - daemon_id is NOT passed through to the Rust schema (it's
           consumed entirely by the owner-resolution logic)
         - topic becomes the filename stem (the Wave 3 wiring will
           handle file I/O — aliases.rs just needs to populate the
           output struct's `filename_stem` field or equivalent)

       map_list_scratchpad_params:
         - cwd → passthrough (the ONLY TS parameter)

       map_code_review_params:
         - cwd → passthrough
         - uncommitted bool → passed through as-is
         - base_branch String → passed through
         - commit_sha String → passed through
         - timeout_ms → passthrough
         - NOTE: The TS `code_review` invokes `codex review` which
           reads the git state itself. The Rust `review_request` does
           NOT accept a `diff` or `context` parameter — it produces
           the diff internally from cwd+base_branch+commit_sha+uncommitted.
           Do NOT invent `diff` or `context` fields.

   (g) Unit tests in a `#[cfg(test)] mod tests { ... }` block.
       Minimum required tests (the reality_test enforces all five):

       #[test]
       fn spawn_daemon_gemini_maps_to_agent_gemini() {
           let p = SpawnDaemonParams { target: "gemini".into(), session_name: Some("x".into()), cwd: None, timeout_ms: None };
           let out = map_spawn_daemon_params(p).unwrap();
           assert_eq!(out.agent, "gemini");
           assert_eq!(out.name, Some("x".into()));
       }

       #[test]
       fn spawn_daemon_codex_maps_to_agent_codex() {
           let p = SpawnDaemonParams { target: "codex".into(), session_name: None, cwd: None, timeout_ms: None };
           let out = map_spawn_daemon_params(p).unwrap();
           assert_eq!(out.agent, "codex");
       }

       #[test]
       fn spawn_daemon_claude_rejected() {
           let p = SpawnDaemonParams { target: "claude".into(), session_name: None, cwd: None, timeout_ms: None };
           let err = map_spawn_daemon_params(p).unwrap_err();
           assert!(matches!(err, AliasMappingError::InvalidTarget(_)));
       }

       #[test]
       fn ask_daemon_preserves_prefix() {
           let p = AskDaemonParams { daemon_id: "gd_session_abc".into(), question: "hi".into(), timeout_ms: None };
           let out = map_ask_daemon_params(p).unwrap();
           assert_eq!(out.name, "gd_session_abc");
           assert_eq!(out.message, "hi");
       }

       #[test]
       fn write_scratchpad_owner_derived_from_gd_prefix() {
           let p = WriteScratchpadParams {
               topic: "notes".into(),
               content: "hello".into(),
               cwd: None,
               owner: None,
               daemon_id: Some("gd_session_xyz".into()),
           };
           let out = map_write_scratchpad_params(p).unwrap();
           assert!(out.owner.starts_with("gemini-"));
       }

4. Run `cargo check -p mcp-tools --manifest-path daemon/Cargo.toml`.
   Must succeed with zero new warnings.

5. Run `cargo test -p mcp-tools --manifest-path daemon/Cargo.toml`.
   All tests in aliases::tests must pass.

6. Run `cargo build --release --manifest-path daemon/Cargo.toml` as a
   final sanity check.

═══════════════════════════════════════════
CLOSING CEREMONY — all three are required
═══════════════════════════════════════════

7. COMMIT with the exact message:
   chore(3.1.0): T-002 add alias parameter-mapping types and conversion functions

8. WRITE THE SENTINEL. This is your explicit completion report:

   cat > .triumvirate/TASK_COMPLETE.json <<EOF
   {
     "task_id": "T-002",
     "commit_sha": "$(git rev-parse HEAD)",
     "result": "ok",
     "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
     "commit_message": "chore(3.1.0): T-002 add alias parameter-mapping types and conversion functions"
   }
   EOF

9. NOTIFY the daemon via HTTP (may fail harmlessly until T-004B):

   curl -sf -X POST \
     -H "Authorization: Bearer $TRIUMVIRATE_TOKEN" \
     -H "Content-Type: application/json" \
     --data-binary @.triumvirate/TASK_COMPLETE.json \
     "http://localhost:$TRIUMVIRATE_HTTP_PORT/abe/task-complete" \
     || echo "HTTP notify failed — sentinel and commit remain as fallback"

═══════════════════════════════════════════
After step 9 you are DONE. Do not run additional verification.
Do not explore further. Do not start new work.
═══════════════════════════════════════════

CONSTRAINTS (hard stops):
- Do NOT modify ANY Cargo.toml file. Not mcp-tools/Cargo.toml, not
  daemon/Cargo.toml, not any per-crate manifest. Use plain struct
  literals for param structs (no #[derive(Deserialize)] macros). Use
  a manual `impl std::fmt::Display` for the AliasMappingError enum
  (NOT thiserror — it is NOT in [workspace.dependencies]; trying to
  add it will fail).
- Do NOT register any tool with the tool_router macro. That's Wave 3.
- Do NOT modify McpBridge. Do NOT touch main.rs.
- Do NOT touch daemon-core, daemon-http, or any crate other than
  mcp-tools.
- Do NOT invent alias names not in the canonical matrix. Exactly 10
  alias mapping functions, no more, no fewer.
- Do NOT invent parameter names not in the canonical TS schemas from
  specs/MCP_CONSOLIDATION.md lines 60-74.
- Do NOT add a `diff` or `context` field to the code_review mapping.
  The TS `code_review` does NOT accept those — it accepts
  cwd/uncommitted/base_branch/commit_sha.
- Do NOT treat pythia_query or pythia_corpus_health as aliases. They
  are OUT OF SCOPE (see spec lines 71-72 and 132-133). A separate
  `pythia` MCP server handles them directly.

HOW YOU'LL KNOW YOU'RE DONE:
- daemon/crates/mcp-tools/src/aliases.rs exists
- grep -q 'pub mod aliases' daemon/crates/mcp-tools/src/lib.rs returns 0
- grep -c '^pub fn map_' daemon/crates/mcp-tools/src/aliases.rs returns 10
- cargo check -p mcp-tools --manifest-path daemon/Cargo.toml exits 0
- cargo test -p mcp-tools --manifest-path daemon/Cargo.toml exits 0
- The five named tests pass: spawn_daemon_gemini_maps_to_agent_gemini,
  spawn_daemon_codex_maps_to_agent_codex, spawn_daemon_claude_rejected,
  ask_daemon_preserves_prefix, write_scratchpad_owner_derived_from_gd_prefix
- .triumvirate/TASK_COMPLETE.json exists with the commit SHA
- Exactly one new commit matches commit_format
```

---

## 3. Proposed ContractFields (JSON)

```json
{
  "task_id": "T-002",
  "req_ids": ["REQ-A1", "REQ-A2"],
  "wave": 0,
  "file_policy": "default-deny",
  "allowed_files": [
    "daemon/crates/mcp-tools/src/aliases.rs",
    "daemon/crates/mcp-tools/src/lib.rs",
    ".triumvirate/TASK_COMPLETE.json"
  ],
  "forbidden_files": [
    "daemon/Cargo.toml",
    "daemon/Cargo.lock",
    "daemon/crates/mcp-tools/Cargo.toml",
    "daemon/crates/agent-adapter/**",
    "daemon/crates/agent-worker/**",
    "daemon/crates/daemon-core/**",
    "daemon/crates/daemon-http/**",
    "daemon/crates/fallback-outbox/**",
    "daemon/crates/fleet/**",
    "daemon/crates/ledger/**",
    "daemon/crates/mcp-bridge/**",
    "daemon/crates/peer-review/**",
    "daemon/crates/shared-types/**",
    "daemon/crates/triumvirate/**",
    "mcp-server/**",
    "dashboard/**",
    "docs/**",
    "specs/**",
    ".github/**",
    ".git/**",
    "CHANGELOG.md",
    "ROADMAP.md"
  ],
  "allowed_commands": [
    ["bash", "-c"],
    ["sh", "-c"],
    ["cargo", "check", "-p", "mcp-tools", "--manifest-path", "daemon/Cargo.toml"],
    ["cargo", "test", "-p", "mcp-tools", "--manifest-path", "daemon/Cargo.toml"],
    ["cargo", "check", "--workspace", "--manifest-path", "daemon/Cargo.toml"],
    ["cargo", "build", "--release", "--manifest-path", "daemon/Cargo.toml"],
    ["cargo", "fmt", "--all", "--manifest-path", "daemon/Cargo.toml"],
    ["grep", "-rn"],
    ["grep", "-n"],
    ["grep", "-c"],
    ["grep", "-E"],
    ["grep", "-q"],
    ["git", "add"],
    ["git", "commit"],
    ["git", "status"],
    ["git", "diff"],
    ["git", "rev-parse"],
    ["chmod", "+x"],
    ["ls", "-la"],
    ["test", "-x"],
    ["test", "-f"],
    ["cat"],
    ["curl", "-sf", "-X", "POST"],
    ["date", "-u"]
  ],
  "forbidden_commands": [
    ["git", "push"],
    ["git", "tag"],
    ["git", "reset"],
    ["git", "rebase"],
    ["rm", "-rf"],
    ["cargo", "install"],
    ["cargo", "publish"],
    ["cargo", "update"],
    ["npm"],
    ["pnpm"]
  ],
  "commit_format": "^chore\\(3\\.1\\.0\\): T-002 add alias parameter-mapping types and conversion functions$",
  "test_command": "bash -c 'cargo check -p mcp-tools --manifest-path daemon/Cargo.toml && cargo test -p mcp-tools --manifest-path daemon/Cargo.toml && test -f daemon/crates/mcp-tools/src/aliases.rs && grep -q \"pub mod aliases\" daemon/crates/mcp-tools/src/lib.rs && [ \"$(grep -c \"^pub fn map_\" daemon/crates/mcp-tools/src/aliases.rs)\" = \"10\" ]'",
  "task_timeout_sec": 1800,
  "done_when": "daemon/crates/mcp-tools/src/aliases.rs exists with all 10 alias mapping functions (map_spawn_daemon_params, map_ask_daemon_params, map_dismiss_daemon_params, map_list_daemons_params, map_send_message_params, map_get_response_params, map_list_jobs_params, map_write_scratchpad_params, map_list_scratchpad_params, map_code_review_params). mcp-tools/src/lib.rs declares `pub mod aliases;`. Unit tests exist and pass for: spawn_daemon gemini valid path, spawn_daemon codex valid path, spawn_daemon claude error path, ask_daemon prefix preservation, write_scratchpad owner derivation from gd_ prefix. cargo check -p mcp-tools --manifest-path daemon/Cargo.toml exits 0 with zero new warnings. cargo test -p mcp-tools --manifest-path daemon/Cargo.toml exits 0. Exactly one new commit landed matching commit_format. .triumvirate/TASK_COMPLETE.json exists with the commit SHA.",
  "reality_test": "All of these must be true after the commit: (1) test -f daemon/crates/mcp-tools/src/aliases.rs exits 0; (2) grep -q 'pub mod aliases' daemon/crates/mcp-tools/src/lib.rs exits 0; (3) grep -c '^pub fn map_' daemon/crates/mcp-tools/src/aliases.rs returns exactly 10; (4) cargo check -p mcp-tools --manifest-path daemon/Cargo.toml exits 0; (5) cargo test -p mcp-tools --manifest-path daemon/Cargo.toml exits 0; (6) The five named unit tests pass (spawn_daemon_gemini_maps_to_agent_gemini, spawn_daemon_codex_maps_to_agent_codex, spawn_daemon_claude_rejected, ask_daemon_preserves_prefix, write_scratchpad_owner_derived_from_gd_prefix); (7) grep -q 'pythia_query\\|pythia_corpus_health' daemon/crates/mcp-tools/src/aliases.rs must NOT match (those are out of scope); (8) .triumvirate/TASK_COMPLETE.json exists with a commit_sha field."
}
```

---

## 4. Relevant BACKEND_STRUCTURE sections

`BACKEND_STRUCTURE.md` lines 146-167 contain the full alias matrix showing every TS→Rust mapping. The worker should treat this as secondary documentation; the primary source of truth is `specs/MCP_CONSOLIDATION.md` lines 176-195. If the two disagree, the spec wins.

Key structural facts from BACKEND_STRUCTURE.md:
- aliases.rs is a sibling module to inter_agent.rs, abe.rs, fleet.rs, knowledge.rs, review.rs, gemini_query.rs inside mcp-tools
- Each alias routes to a canonical handler (e.g., `spawn_daemon` → `spawn_session`)
- Each alias logs `tracing::info!("tool_alias", old_name, new_name)` — this is a Wave 3 wiring detail, NOT part of T-002 (T-002 defines types and mappings, not the logging at invocation time)
- 10 aliases total — exactly, no more

---

## 5. Dependency Context

**Prior committed tasks this depends on:**

- `T-000` — committed at `882a113` on 2026-04-09. Established baseline workspace version 3.1.0 and `--manifest-path` command pattern.
- Baseline SHA for the T-002 worktree: `882a1134537010c98d4505c9ad7ef3609bb8af4b`.

**Parallel sibling:** T-001 (ObservabilityBus + DaemonMetrics relocation). T-001 and T-002 are dispatched in parallel because they touch disjoint file sets (T-001: daemon-core + main.rs; T-002: mcp-tools). Neither depends on the other.

**Downstream dependents:** Wave 3 T-011 (alias registration with tool_router) consumes these mapping functions when wiring aliases to the McpBridge.

---

## 6. Audit Ask

Apply the Phase 5.3 audit checklist from `~/.claude/skills/goatrodeo.md` (12 checks plus check #13 for the three-ceremony closing block). Return the JSON verdict shape. REJECTED on any critical or high finding. Read `specs/MCP_CONSOLIDATION.md` lines 60-74 and 176-195 directly to verify alias fidelity. Cap audit at 2 rounds per Rule A; escalate past 2 rather than grinding.
