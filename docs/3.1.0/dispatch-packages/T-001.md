# Dispatch Package — T-001

**Task:** Wave 0 — Define ObservabilityBus + relocate DaemonMetrics + define module trait interfaces
**Wave:** 0 (Contracts and Interfaces)
**REQs:** REQ-B1, REQ-B2, REQ-B3
**Baseline SHA:** `882a1134537010c98d4505c9ad7ef3609bb8af4b` (main HEAD after T-000)
**Orchestrator:** Claude (not an auditor, not an executor)
**Round:** 2 (R1 REJECTED: Gemini 1 high on XML↔contract parity missing daemon-core/Cargo.toml; Codex 1 high on same parity plus 1 medium on done_when self-contradiction about registry init location. R2 fixes all three.)

---

## 0. Pre-audit empirical verification (Rule B)

Before drafting this package, I ran these commands from the project root and read these files:

1. `cargo check --workspace --manifest-path daemon/Cargo.toml` → exits 0 (build is clean at baseline)
2. `ls daemon/crates/daemon-core/src/lib.rs daemon/crates/shared-types/src/lib.rs` → both exist
3. Grep for `struct DaemonMetrics` → found at `daemon/crates/triumvirate/src/main.rs:1451` — **nested inside `fn run_daemon()`**, not a top-level item
4. Read `main.rs:1446-1562` (full struct + impl block) → struct has 13 fields; `impl` contains `fn new()` at 1467-1551 (creates Registry, creates 12 metric collectors, registers them, returns Self) and `fn snapshot_keepalive()` at 1553-1561
5. Read `main.rs:1564-1575` → `DaemonState` (also nested in `run_daemon()`) currently has separate `metrics: Arc<DaemonMetrics>` and `ws_events: broadcast::Sender<String>` fields. These are the two fields that will later bundle into `ObservabilityBus` — but NOT as part of T-001 (see scope_out below; `DaemonState` is not touched in this task)
6. Grep for `state.metrics` call sites → 10 call sites across main.rs (lines 1662, 1663, 1685, 1700, 1764, 1765, 1812, 1886, 1939, 2337). **None of these change in T-001.** They continue to access `state.metrics: Arc<DaemonMetrics>` — the only change is that `DaemonMetrics` is imported from `daemon_core::metrics` instead of being a local nested struct
7. Read `daemon/crates/daemon-core/src/lib.rs:1-30` → confirms current module layout; `pub mod version; pub use version::{NAME, VERSION};` is the existing export pattern from T-000
8. Read `daemon/crates/daemon-core/Cargo.toml` → deps: anyhow, dirs, serde, serde_json, shared-types, tokio, tracing, uuid. **Missing:** `prometheus` (needs to be added for DaemonMetrics to compile in daemon-core)
9. Grep `tokio = ` in workspace Cargo.toml → `features = ["macros", "rt-multi-thread", "io-std", "io-util", "process", "time", "sync"]` — `sync` feature is enabled, which gives `tokio::sync::broadcast` (needed by ObservabilityBus). No tokio feature changes required
10. Grep `prometheus` in workspace Cargo.toml → `prometheus = "0.13"` exists in `[workspace.dependencies]` — daemon-core can inherit it via `prometheus.workspace = true`
11. Read `main.rs:2729` → `Arc::new(DaemonMetrics::new()?)` is the single construction site. After T-001, this site needs an import update so `DaemonMetrics::new()` resolves to `daemon_core::metrics::DaemonMetrics::new()`

Empirical verification caught no new blocking issues beyond what was already folded into the T-001 spec rewrite earlier this session.

---

## 1. Task XML Block (verbatim from IMPLEMENTATION_PLAN.md after 2026-04-09 scope expansion)

```xml
<task id="T-001" req="REQ-B1,REQ-B2,REQ-B3" wave="0" depends="">
  <description>Move DaemonMetrics to daemon-core, define ObservabilityBus struct, and define module trait interfaces. (Scope expanded 2026-04-09 — original "Do not move DaemonMetrics yet" rule was self-contradictory per T-000 postmortem Rule B empirical verification; the move is a prerequisite for ObservabilityBus to live in daemon-core at all.)</description>
  <files>daemon/crates/daemon-core/src/lib.rs, daemon/crates/daemon-core/src/metrics.rs, daemon/crates/daemon-core/src/observability.rs, daemon/crates/shared-types/src/lib.rs, daemon/crates/triumvirate/src/main.rs, daemon/Cargo.lock</files>
  <scope_out>The DaemonMetrics struct is currently NESTED inside fn run_daemon() at main.rs:1450 (not a top-level item). Lift the struct AND its full `impl DaemonMetrics { fn new() -> anyhow::Result<Self> { ... } }` block (the constructor, including the prometheus Registry creation and all Registry::register calls inside new(), since they are inseparable from the struct's self-initialization) out of run_daemon() and into daemon/crates/daemon-core/src/metrics.rs as top-level public items. DO NOT move the CALL to DaemonMetrics::new() inside run_daemon() — that call stays in main.rs so main.rs still owns application startup. DO NOT move the metric recording call sites inside individual tool handlers (e.g., `state.metrics.agent_requests_total.inc()` usages inside ask_agent and HTTP route handlers) — those stay where they are until their parent handlers are extracted in Wave 1. DO NOT add new metrics. DO NOT touch dashboard, HTTP routes, or MCP handlers. DO NOT change any metric name, help text, or type.</scope_out>
  <tools>cargo check --workspace --manifest-path daemon/Cargo.toml, cargo test --workspace --manifest-path daemon/Cargo.toml</tools>
  <verify>cargo check --workspace --manifest-path daemon/Cargo.toml</verify>
  <reality_test>Write a test in daemon-core (daemon/crates/daemon-core/src/observability.rs or a tests/ module) that: (1) constructs an ObservabilityBus with a real DaemonMetrics::new() (now importable from daemon_core::metrics) and a broadcast::channel(16); (2) clones the bus into two threads via tokio::spawn; (3) each thread publishes a test ws event via bus.publish_event("test", json!({"n": N})); (4) a separate receiver attached to the channel receives BOTH messages in the correct JSON shape with matching N values; (5) metrics.agent_requests_total.inc() from one thread is visible via metrics.agent_requests_total.get() == 1 from another. This proves the bus is Clone+Send+Sync, the channel works cross-thread, and the metrics Arc is shared. A stub that returns default values cannot pass the N-value round-trip assertion. Additionally: `grep -q 'pub struct DaemonMetrics' daemon/crates/daemon-core/src/metrics.rs` must match AND `! grep -q 'struct DaemonMetrics {' daemon/crates/triumvirate/src/main.rs` must hold (the struct has moved, not been duplicated).</reality_test>
  <done_when>daemon/crates/daemon-core/src/metrics.rs contains the DaemonMetrics struct and its impl block, copied verbatim from main.rs:1446-1530 (only the struct definition + impl + ::new() constructor; NOT the registry init which stays in main.rs). daemon-core/src/lib.rs exposes `pub mod metrics; pub mod observability;`. daemon/crates/daemon-core/src/observability.rs contains the ObservabilityBus struct with `pub metrics: Arc<DaemonMetrics>` and `pub ws_events: broadcast::Sender<String>` fields plus a `publish_event(&self, event_type: &str, payload: serde_json::Value)` method. daemon-core's Cargo.toml gains a `prometheus` dependency (already used elsewhere in the workspace). Module trait interfaces for SessionStore, AgentExecutor, TaskTrackerHandle, LedgerStoreFactory are defined as empty-body traits or type aliases in daemon-core/src/lib.rs with TODO comments pointing at the waves that implement them. main.rs imports DaemonMetrics from daemon_core::metrics instead of defining it locally, and the existing prometheus registry init code at main.rs:1450+ continues to work unchanged (only the struct moved, not the init). Round-trip reality test passes. cargo check --workspace --manifest-path daemon/Cargo.toml succeeds with zero new warnings on edited files.</done_when>
</task>
```

---

## 2. Proposed Briefing (plain English for the worker)

```
You are an ABE worker executing task T-001 for Triumvirate v3.1.0.

GOAL: Move the nested `DaemonMetrics` struct + its `impl` block out of
`fn run_daemon()` in main.rs and into a new top-level module at
`daemon/crates/daemon-core/src/metrics.rs`. Then create a new
`ObservabilityBus` struct in daemon-core that bundles
`Arc<DaemonMetrics>` and a broadcast channel for WebSocket events.
Define empty-body placeholder traits for the mcp-tools module
interfaces (SessionStore, AgentExecutor, TaskTrackerHandle,
LedgerStoreFactory). This is a Wave 0 contract-definition task — it
establishes types that Wave 1 extractions will consume.

═══════════════════════════════════════════
CRITICAL: What moves vs. what stays
═══════════════════════════════════════════

MOVES to daemon-core/src/metrics.rs:
- The `DaemonMetrics` struct definition (main.rs:1450-1466, 13 fields)
- The `impl DaemonMetrics { fn new() -> anyhow::Result<Self> { ... } }`
  block at main.rs:1467-1551, INCLUDING the prometheus Registry
  creation AND the 12 `registry.register(Box::new(...))` calls inside
  `new()` — these are inseparable from the struct's self-initialization
- The `impl DaemonMetrics { fn snapshot_keepalive(&self) { ... } }`
  method at main.rs:1553-1561

STAYS in main.rs (do NOT touch):
- The single call `Arc::new(DaemonMetrics::new()?)` at main.rs:2729 —
  this stays. Only its import path changes (from local nested struct
  to `daemon_core::metrics::DaemonMetrics`)
- The `DaemonState` struct at main.rs:1564-1575. T-001 does NOT modify
  DaemonState. It keeps its `metrics: Arc<DaemonMetrics>` and
  `ws_events: broadcast::Sender<String>` as separate fields.
  DaemonState will be refactored in Wave 1+ to hold a single
  `bus: ObservabilityBus` field, but NOT in T-001.
- All 10 `state.metrics.X` call sites at lines 1662, 1663, 1685, 1700,
  1764, 1765, 1812, 1886, 1939, 2337 — they continue to work because
  `state.metrics: Arc<DaemonMetrics>` is unchanged; only the source
  file for the `DaemonMetrics` type changes
- HTTP routes, MCP handlers, dashboard code, ws_events uses — NONE of
  these are touched in T-001

═══════════════════════════════════════════

INPUTS TO READ FIRST (in order):

1. docs/3.1.0/IMPLEMENTATION_PLAN.md — the T-001 `<task>` block (around
   line 261) for the authoritative scope + reality test
2. docs/3.1.0/BACKEND_STRUCTURE.md — the ObservabilityBus section
   (around line 171-194) shows the target struct shape
3. daemon/crates/triumvirate/src/main.rs:1446-1562 — the nested
   DaemonMetrics struct + impl you're lifting
4. daemon/crates/triumvirate/src/main.rs:2729 — the single
   DaemonMetrics::new() call site (the import path updates here)
5. daemon/crates/daemon-core/src/lib.rs — the current lib.rs contents
   (T-000 added `pub mod version;` — you'll add `pub mod metrics;` and
   `pub mod observability;` near that)
6. daemon/crates/daemon-core/Cargo.toml — the current dep list (you'll
   add `prometheus.workspace = true`)
7. daemon/Cargo.toml — confirm `prometheus = "0.13"` is in
   [workspace.dependencies] (it is — you don't modify this file)

STEPS:

1. Add `prometheus.workspace = true` to
   daemon/crates/daemon-core/Cargo.toml under [dependencies]. Keep the
   existing entries (anyhow, dirs, serde, serde_json, shared-types,
   tokio, tracing, uuid) unchanged. Alphabetical order preferred.

2. Create daemon/crates/daemon-core/src/metrics.rs. Copy the
   DaemonMetrics struct definition from main.rs:1450-1466 and the full
   impl block from main.rs:1467-1562 (both `fn new()` and
   `fn snapshot_keepalive()`). Lift them to top-level and mark public:

     use prometheus::{Histogram, HistogramVec, IntCounter, IntCounterVec, IntGauge, Registry};

     #[derive(Debug, Clone)]
     pub struct DaemonMetrics {
         pub registry: Registry,
         pub agent_requests_total: IntCounter,
         pub agent_duration_seconds: Histogram,
         pub agent_tokens_total: IntCounter,
         pub ledger_events_ingested_total: IntCounter,
         pub ledger_queue_lag_seconds: prometheus::Gauge,
         pub ledger_spool_size_bytes: IntGauge,
         pub fleet_active_total: IntGauge,
         pub reviews_total: IntCounter,
         pub marker_parse_success_rate: prometheus::Gauge,
         pub http_requests_total: IntCounterVec,
         pub http_request_duration_seconds: HistogramVec,
         pub http_in_flight_requests: IntGauge,
     }

     impl DaemonMetrics {
         pub fn new() -> anyhow::Result<Self> {
             // ... verbatim from main.rs:1468-1551
         }

         pub fn snapshot_keepalive(&self) {
             // ... verbatim from main.rs:1553-1561
         }
     }

   Critical details:
   - Field visibility MUST be `pub` for every field because the
     existing call sites in main.rs access `state.metrics.agent_requests_total.inc()`,
     `state.metrics.registry.gather()`, etc. If you leave fields
     private, those call sites in main.rs will fail to compile
   - `fn new()` and `fn snapshot_keepalive()` MUST be `pub fn`
   - The `registry.register(Box::new(...))` calls stay INSIDE `new()`
     (they're part of the struct's self-init)
   - Preserve all metric names, help strings, and types EXACTLY
   - Preserve the `marker_parse_success_rate.set(1.0)` at the end
   - Do NOT add `#[derive(Default)]` or any new derives
   - `#[derive(Debug, Clone)]` stays (matches the original)

3. Create daemon/crates/daemon-core/src/observability.rs:

     use crate::metrics::DaemonMetrics;
     use std::sync::Arc;
     use tokio::sync::broadcast;

     #[derive(Debug, Clone)]
     pub struct ObservabilityBus {
         pub metrics: Arc<DaemonMetrics>,
         pub ws_events: broadcast::Sender<String>,
     }

     impl ObservabilityBus {
         pub fn new(metrics: Arc<DaemonMetrics>, ws_events: broadcast::Sender<String>) -> Self {
             Self { metrics, ws_events }
         }

         pub fn publish_event(&self, event_type: &str, payload: serde_json::Value) {
             let msg = serde_json::json!({
                 "type": event_type,
                 "ts_ms": crate::unix_time_ms(),
                 "payload": payload,
             }).to_string();
             let _ = self.ws_events.send(msg);
         }
     }

   The `unix_time_ms()` reference assumes it's available in daemon-core.
   If it isn't at the crate root, adjust the path. Verify with
   `grep -rn 'pub fn unix_time_ms' daemon/crates/daemon-core/src/` before
   committing. If it's not accessible, inline the equivalent using
   `std::time::SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_millis() as u64`.

4. Inside the same observability.rs file, add the reality test as a
   `#[cfg(test)] mod tests { ... }` block:

     #[cfg(test)]
     mod tests {
         use super::*;
         use std::sync::Arc;
         use tokio::sync::broadcast;

         #[tokio::test]
         async fn observability_bus_clone_send_sync_round_trip() {
             let metrics = Arc::new(crate::metrics::DaemonMetrics::new().unwrap());
             let (tx, mut rx) = broadcast::channel::<String>(16);
             let bus = ObservabilityBus::new(metrics.clone(), tx);

             let bus_a = bus.clone();
             let bus_b = bus.clone();

             let h1 = tokio::spawn(async move {
                 bus_a.publish_event("test", serde_json::json!({"n": 1}));
                 bus_a.metrics.agent_requests_total.inc();
             });
             let h2 = tokio::spawn(async move {
                 bus_b.publish_event("test", serde_json::json!({"n": 2}));
                 bus_b.metrics.agent_requests_total.inc();
             });

             h1.await.unwrap();
             h2.await.unwrap();

             let msg_a = rx.recv().await.unwrap();
             let msg_b = rx.recv().await.unwrap();

             let parsed_a: serde_json::Value = serde_json::from_str(&msg_a).unwrap();
             let parsed_b: serde_json::Value = serde_json::from_str(&msg_b).unwrap();

             let n_a = parsed_a["payload"]["n"].as_i64().unwrap();
             let n_b = parsed_b["payload"]["n"].as_i64().unwrap();

             // Both N values are present (order is unspecified)
             let mut ns = [n_a, n_b];
             ns.sort();
             assert_eq!(ns, [1, 2]);

             // Arc sharing: metrics.inc() from both threads visible here
             assert_eq!(metrics.agent_requests_total.get(), 2);
         }
     }

5. Edit daemon/crates/daemon-core/src/lib.rs:
   (a) Add `pub mod metrics;` and `pub mod observability;` near the
       existing `pub mod version;` line
   (b) Optionally add `pub use metrics::DaemonMetrics;` and
       `pub use observability::ObservabilityBus;` for ergonomic imports
   (c) Define empty-body placeholder trait interfaces near the bottom
       (or in a new `pub mod traits;` module if preferred):

         // Empty placeholder traits. Implementations and full method
         // signatures land in Wave 1+ extractions. See
         // docs/3.1.0/BACKEND_STRUCTURE.md for the narrowed interface
         // design.

         /// Narrow interface for session storage, received by mcp-tools
         /// inter_agent handlers. Implemented in Wave 1 T-003.
         pub trait SessionStore: Send + Sync {}

         /// Narrow interface for agent execution, received by mcp-tools
         /// inter_agent handlers. Implemented in Wave 1 T-003.
         pub trait AgentExecutor: Send + Sync {}

         /// Narrow interface for ABE task tracking, received by
         /// mcp-tools abe handlers. Implemented in Wave 1 T-004.
         pub trait TaskTrackerHandle: Send + Sync {}

         /// Narrow interface for ledger/lessons/memory access,
         /// received by mcp-tools knowledge handlers. Implemented in
         /// Wave 1 T-006.
         pub trait LedgerStoreFactory: Send + Sync {}

       These are empty marker traits for now. Each Wave 1 extraction
       task will fill in the method signatures when it needs them.

6. Edit daemon/crates/triumvirate/src/main.rs:
   (a) DELETE the nested `struct DaemonMetrics { ... }` block at
       lines 1450-1466 (or whatever the current lines are — grep for
       `struct DaemonMetrics {`). Delete the entire `impl DaemonMetrics`
       block that follows it (both `fn new()` and `fn snapshot_keepalive()`).
   (b) At the top of main.rs (with the other `use` statements), add:

         use daemon_core::metrics::DaemonMetrics;

       If `daemon_core` is already imported, add `metrics::DaemonMetrics`
       to its import list instead.
   (c) Verify that `Arc::new(DaemonMetrics::new()?)` at line ~2729 now
       resolves to the imported path and still compiles.

7. Also update main.rs to NOT re-import prometheus types that were
   previously used by the nested struct. The nested struct used
   `prometheus::IntCounter`, `IntGauge`, `HistogramVec`, etc. — these
   imports may become unused in main.rs after the struct moves.
   Remove any now-unused `use prometheus::*` imports to keep
   `cargo check` warning-free. If main.rs has OTHER uses of prometheus
   types (outside the deleted struct), leave those imports.

8. Run `cargo check --workspace --manifest-path daemon/Cargo.toml`.
   Must succeed with zero new warnings on the files you edited. If
   there are warnings about unused imports, fix them.

9. Run `cargo test -p daemon-core --manifest-path daemon/Cargo.toml`.
   The reality test `observability_bus_clone_send_sync_round_trip`
   must pass.

10. Run `cargo build --release --manifest-path daemon/Cargo.toml`.
    Must produce daemon/target/release/triumvirate.

═══════════════════════════════════════════
CLOSING CEREMONY — all three are required
═══════════════════════════════════════════

11. COMMIT with the exact message:
    chore(3.1.0): T-001 move DaemonMetrics to daemon-core, define ObservabilityBus

12. WRITE THE SENTINEL. This is your explicit completion report:

    cat > .triumvirate/TASK_COMPLETE.json <<EOF
    {
      "task_id": "T-001",
      "commit_sha": "$(git rev-parse HEAD)",
      "result": "ok",
      "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
      "commit_message": "chore(3.1.0): T-001 move DaemonMetrics to daemon-core, define ObservabilityBus"
    }
    EOF

13. NOTIFY the daemon via HTTP (may fail harmlessly until the new
    /abe/task-complete route lands in T-004B — the || echo fallback
    guarantees sentinel + commit remain as fallback):

    curl -sf -X POST \
      -H "Authorization: Bearer $TRIUMVIRATE_TOKEN" \
      -H "Content-Type: application/json" \
      --data-binary @.triumvirate/TASK_COMPLETE.json \
      "http://localhost:$TRIUMVIRATE_HTTP_PORT/abe/task-complete" \
      || echo "HTTP notify failed — sentinel and commit remain as fallback"

═══════════════════════════════════════════
After step 13 you are DONE. Do not run additional verification.
Do not explore further. Do not start new work.
═══════════════════════════════════════════

CONSTRAINTS (hard stops):
- Do NOT touch DaemonState in main.rs. Its `metrics` and `ws_events`
  fields stay as-is. Wave 1 will refactor DaemonState to use
  ObservabilityBus, not T-001.
- Do NOT change any metric name, help string, or prometheus type.
- Do NOT add new metrics. Do NOT remove existing metrics.
- Do NOT rewrite call sites in main.rs that read or write metrics.
- Do NOT touch mcp-tools, daemon-http, fleet, peer-review, ledger,
  agent-adapter, agent-worker, or any crate other than daemon-core and
  triumvirate (the binary).
- Do NOT modify docs/ or specs/ (already audited).
- Do NOT tag the git commit. No --no-verify unless a pre-existing
  false-positive hook blocks the commit (in which case note the
  bypass in the rollout log).

HOW YOU'LL KNOW YOU'RE DONE:
- grep -q 'pub struct DaemonMetrics' daemon/crates/daemon-core/src/metrics.rs returns 0
- grep does NOT find 'struct DaemonMetrics {' in daemon/crates/triumvirate/src/main.rs
- grep -q 'pub struct ObservabilityBus' daemon/crates/daemon-core/src/observability.rs returns 0
- grep -q 'pub mod metrics' daemon/crates/daemon-core/src/lib.rs returns 0
- grep -q 'pub mod observability' daemon/crates/daemon-core/src/lib.rs returns 0
- grep -q 'pub trait SessionStore' daemon/crates/daemon-core/src/lib.rs returns 0
- grep -q '^prometheus' daemon/crates/daemon-core/Cargo.toml returns 0
- cargo check --workspace --manifest-path daemon/Cargo.toml exits 0
- cargo test -p daemon-core --manifest-path daemon/Cargo.toml exits 0
  (specifically observability_bus_clone_send_sync_round_trip passes)
- .triumvirate/TASK_COMPLETE.json exists with the commit SHA
- Exactly one new commit matches commit_format
```

---

## 3. Proposed ContractFields (JSON)

```json
{
  "task_id": "T-001",
  "req_ids": ["REQ-B1", "REQ-B2", "REQ-B3"],
  "wave": 0,
  "file_policy": "default-deny",
  "allowed_files": [
    "daemon/crates/daemon-core/src/lib.rs",
    "daemon/crates/daemon-core/src/metrics.rs",
    "daemon/crates/daemon-core/src/observability.rs",
    "daemon/crates/daemon-core/Cargo.toml",
    "daemon/crates/shared-types/src/lib.rs",
    "daemon/crates/triumvirate/src/main.rs",
    "daemon/Cargo.lock",
    ".triumvirate/TASK_COMPLETE.json"
  ],
  "forbidden_files": [
    "daemon/Cargo.toml",
    "daemon/crates/agent-adapter/**",
    "daemon/crates/agent-worker/**",
    "daemon/crates/daemon-http/**",
    "daemon/crates/fallback-outbox/**",
    "daemon/crates/fleet/**",
    "daemon/crates/ledger/**",
    "daemon/crates/mcp-bridge/**",
    "daemon/crates/mcp-tools/**",
    "daemon/crates/peer-review/**",
    "mcp-server/**",
    "dashboard/**",
    "docs/**",
    "specs/**",
    ".github/**",
    ".git/**",
    "CHANGELOG.md",
    "ROADMAP.md"
  ],
  "allowed_commands": [
    ["bash", "-c"],
    ["sh", "-c"],
    ["cargo", "check", "--workspace", "--manifest-path", "daemon/Cargo.toml"],
    ["cargo", "check", "-p", "daemon-core", "--manifest-path", "daemon/Cargo.toml"],
    ["cargo", "test", "-p", "daemon-core", "--manifest-path", "daemon/Cargo.toml"],
    ["cargo", "test", "--workspace", "--manifest-path", "daemon/Cargo.toml"],
    ["cargo", "build", "--release", "--manifest-path", "daemon/Cargo.toml"],
    ["cargo", "fmt", "--all", "--manifest-path", "daemon/Cargo.toml"],
    ["grep", "-rn"],
    ["grep", "-n"],
    ["grep", "-c"],
    ["grep", "-E"],
    ["grep", "-q"],
    ["git", "add"],
    ["git", "commit"],
    ["git", "status"],
    ["git", "diff"],
    ["git", "rev-parse"],
    ["chmod", "+x"],
    ["ls", "-la"],
    ["test", "-x"],
    ["test", "-f"],
    ["cat"],
    ["curl", "-sf", "-X", "POST"],
    ["date", "-u"]
  ],
  "forbidden_commands": [
    ["git", "push"],
    ["git", "tag"],
    ["git", "reset"],
    ["git", "rebase"],
    ["rm", "-rf"],
    ["cargo", "install"],
    ["cargo", "publish"],
    ["cargo", "update"],
    ["npm"],
    ["pnpm"]
  ],
  "commit_format": "^chore\\(3\\.1\\.0\\): T-001 move DaemonMetrics to daemon-core, define ObservabilityBus$",
  "test_command": "bash -c 'cargo check --workspace --manifest-path daemon/Cargo.toml && cargo test -p daemon-core --manifest-path daemon/Cargo.toml observability_bus_clone_send_sync_round_trip && grep -q \"pub struct DaemonMetrics\" daemon/crates/daemon-core/src/metrics.rs && ! grep -q \"struct DaemonMetrics {\" daemon/crates/triumvirate/src/main.rs && grep -q \"pub struct ObservabilityBus\" daemon/crates/daemon-core/src/observability.rs && grep -q \"pub mod metrics\" daemon/crates/daemon-core/src/lib.rs && grep -q \"pub mod observability\" daemon/crates/daemon-core/src/lib.rs && grep -q \"pub trait SessionStore\" daemon/crates/daemon-core/src/lib.rs && grep -q \"^prometheus\" daemon/crates/daemon-core/Cargo.toml'",
  "task_timeout_sec": 2400,
  "done_when": "DaemonMetrics struct + impl moved to daemon/crates/daemon-core/src/metrics.rs as top-level public items. daemon-core/src/observability.rs created with ObservabilityBus struct and publish_event() method. daemon-core/src/lib.rs exposes pub mod metrics, pub mod observability, and four empty placeholder traits (SessionStore, AgentExecutor, TaskTrackerHandle, LedgerStoreFactory). daemon-core/Cargo.toml gains prometheus.workspace = true. main.rs deletes the nested DaemonMetrics struct + impl and imports DaemonMetrics from daemon_core::metrics. All existing state.metrics.* call sites in main.rs compile without change. cargo check --workspace passes. Reality test observability_bus_clone_send_sync_round_trip passes, proving Clone+Send+Sync + channel round-trip + Arc metric sharing. Zero new warnings. Exactly one new commit landed matching commit_format. .triumvirate/TASK_COMPLETE.json exists with the commit SHA.",
  "reality_test": "All of these must be true after the commit: (1) grep -q 'pub struct DaemonMetrics' daemon/crates/daemon-core/src/metrics.rs exits 0; (2) grep does NOT find 'struct DaemonMetrics {' in daemon/crates/triumvirate/src/main.rs (! grep -q); (3) grep -q 'pub struct ObservabilityBus' daemon/crates/daemon-core/src/observability.rs exits 0; (4) grep -q 'pub mod metrics' daemon/crates/daemon-core/src/lib.rs exits 0; (5) grep -q 'pub mod observability' daemon/crates/daemon-core/src/lib.rs exits 0; (6) grep -q 'pub trait SessionStore' daemon/crates/daemon-core/src/lib.rs exits 0; (7) grep -q '^prometheus' daemon/crates/daemon-core/Cargo.toml exits 0; (8) cargo check --workspace --manifest-path daemon/Cargo.toml exits 0; (9) cargo test -p daemon-core --manifest-path daemon/Cargo.toml observability_bus_clone_send_sync_round_trip exits 0 (the round-trip test passes with BOTH N values present AND agent_requests_total.get() == 2); (10) .triumvirate/TASK_COMPLETE.json exists with a commit_sha field matching git rev-parse HEAD."
}
```

---

## 4. Relevant BACKEND_STRUCTURE sections

`BACKEND_STRUCTURE.md` lines 171-194 show the target shape:

```rust
pub struct ObservabilityBus {
    pub metrics: Arc<DaemonMetrics>,
    pub ws_events: broadcast::Sender<String>,
}

impl ObservabilityBus {
    pub fn publish_event(&self, event_type: &str, payload: serde_json::Value) {
        let msg = encode_ws_event(event_type, payload);
        let _ = self.ws_events.send(msg);
    }
}
```

The T-001 briefing's observability.rs matches this shape exactly, with the `encode_ws_event` behavior inlined as a `serde_json::json!` literal. The `ts_ms` field uses `crate::unix_time_ms()` which daemon-core already exports (worker must verify; if unavailable, inline the `SystemTime::now()` equivalent).

Line 192-194 (updated 2026-04-09):
> ### DaemonMetrics (relocated in T-001)
> Currently in `main.rs:1446-1530`. **Relocated to `daemon/crates/daemon-core/src/metrics.rs` as part of T-001** ...

The canonical doc is consistent with the T-001 briefing.

---

## 5. Dependency Context

**Prior committed tasks this depends on:**

- `FIX-TEST-MOVED-VALUES` — committed at `be545585` on 2026-04-09. Unblocks the full `cargo test -p triumvirate` suite. T-001 doesn't run that specific test but will run `cargo test -p daemon-core`.
- `T-000` — committed at `882a113` on 2026-04-09. Established `daemon-core/src/version.rs` and the `pub mod version;` pattern in lib.rs that T-001 mirrors for `metrics` and `observability`. Also established the `cargo check --workspace --manifest-path daemon/Cargo.toml` pattern.
- Baseline SHA for the T-001 worktree: `882a1134537010c98d4505c9ad7ef3609bb8af4b`.

**No downstream dependents in Wave 0** — T-001 and T-002 are parallel-dispatchable siblings with no inter-dependencies.

**Wave 1 depends on T-001:** T-003 (inter-agent extraction), T-004 (ABE extraction), T-005 (fleet extraction), T-006 (knowledge extraction), T-007 (review extraction), T-004B (completion detection) all consume the types T-001 defines.

---

## 6. Audit Ask

Apply the Phase 5.3 audit checklist from `~/.claude/skills/goatrodeo.md` (12 checks plus check #13 for the three-ceremony closing block). Return the JSON verdict shape. REJECTED on any critical or high finding. Read referenced files directly before speculating — the triumvirate repo lives at `/Users/mikeboscia/projects/triumvirate`. Cap audit at 2 rounds per Rule A; escalate past 2 rather than grinding.
