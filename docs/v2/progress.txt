TRIUMVIRATE V2 — PROGRESS TRACKER
==================================

Last updated: 2026-04-05
Current phase: Phase 4-7 buildout (fleet orchestration + resilience hardening)
Next step: Phase 4.1 agent pool + Phase 4.4 FleetWorkflow implementation

COMPLETED
---------
- POC 1/2/3 foundations are live: Claude/Gemini/Codex connectors, parser mapping, web + WS plumbing.
- Routing + digest + quota tracking implemented.
- Memory + steno + workflow persistence implemented.
- Agent supervisor auto-restart with exponential backoff implemented.
- Workflow recovery inspection implemented on startup.
- Fleet task model and lifecycle APIs implemented:
  - `/api/fleet/spawn`
  - `/api/fleet/tasks`
  - `/api/fleet/tasks/claim`
  - `/api/fleet/tasks/complete`
- Fleet worktree orchestration implemented:
  - provision (`git worktree add`) on spawn
  - list via `/api/fleet/worktrees`
  - teardown via `/api/fleet/worktrees/teardown`
- Fleet sequential merge API implemented:
  - `/api/fleet/merge` (ordered branch merge with conflict detection + abort)
- Fleet peer messaging API implemented:
  - `/api/fleet/peer`
- Fleet status API implemented:
  - `/api/fleet/status/{fleet_id}`
- Fleet merge now governance-gated.
- Agent pool scaffold implemented (round-robin instance selection).
- FleetWorkflow scaffold implemented and linked from fleet spawn.
- Prompt memory injection implemented:
  - recent decisions prepended into routed agent turns.
- API backend abstraction scaffold added for future provider-backed agents.
- Governance baseline expanded:
  - default policy file seeded to `~/.triumvirate/policies/default.cedar`
  - daemon-side `daemon/policies/default.cedar` added
- Mock CLI workspace crates added:
  - `mock-claude`, `mock-gemini`, `mock-codex`
  - connector binary override env vars + tests added
- Packaging/docs:
  - `daemon/NOTICE.md`
  - `daemon/README.md`
- Frontend Phase 5.1 scaffold implemented:
  - `daemon/frontend/` with Svelte 5 + Vite + Tailwind baseline
  - build verified via `npm run build`
- Debate scaffold implemented:
  - `DebateWorkflow` in workflow crate
  - `POST /api/debate/start`

IN PROGRESS
-----------
- Phase 5 Svelte dashboard replacement (currently still embedded static HTML)
- Phase 7.4+ deeper integration/chaos tests using mock CLIs

BLOCKED
-------
(nothing blocked)

!! IMPORTANT — READ THIS !!
----------------------------
Three new features were added AFTER the original doc suite (commit 90d1931):
  - FEAT-028: Prometheus /metrics endpoint (Phase 6A step 6.1)
  - FEAT-029: Langfuse LLM observability (Phase 6A steps 6.2 + 6.4)
  - FEAT-030: Cost attribution (Phase 6A step 6.3)

These are in PRD.md, IMPLEMENTATION_PLAN.md, TEST_PLAN.md, and BACKEND_STRUCTURE.md.
The routing_log table now has cost_usd, latency_ms, langfuse_trace_id columns.
Config schema has [langfuse] and [pricing] sections.

Phase 6 in IMPLEMENTATION_PLAN.md was split into 6A (observability) and 6B (debate/governance).
Re-read IMPLEMENTATION_PLAN.md Phase 6 before starting that phase.

ALSO: All code must have comments. Commenting rules added to AGENTS.md:
  - /// doc comments on every public item
  - // inline comments on non-obvious logic
  - // FEAT-XXX at top of each module
  - // Adapted from ... attribution on borrowed patterns

NEXT UP
-------
Phase 5: full Svelte/Tailwind frontend and rust-embed pipeline
Phase 6A: LLM observability (metrics, Langfuse, cost attribution) — NEW
Phase 6B: debate workflow + richer Cedar enforcement
Phase 7: integration and chaos matrix on mock CLIs

NOTES
-----
- All 3 CLIs present: claude at ~/.local/bin/claude, gemini at /opt/homebrew/bin/gemini, codex at ~/.local/bin/codex
- Daemon `target/` ignore already addressed.
- Live smoke validated fleet spawn/worktree/task/peer/teardown APIs on :8080.
- Langfuse instance: https://langfuse.e5btools.com (credentials in ~/.claude/infrastructure.md)
