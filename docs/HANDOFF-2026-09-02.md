# Handoff, 2026-09-02

**Session:** e717582a-4de5-41a5-a409-b0c6f7d6e4d7
**Transcript:** `~/.claude/projects/-Users-michaelboscia-projects-triumvirate/e717582a-4de5-41a5-a409-b0c6f7d6e4d7.jsonl`
**Pushed:** `origin/main` moved `02757a0` to `9913754`, 20 commits.

## What this session did

Built a sight gate so a Triumvirate review can be rejected when the agent never read anything,
then discovered the thing that made it matter: **mandatory peer review had never reviewed
anything.** `enforce_mandatory_peer_review` wrote a database row and immediately stamped it
approved. Grok found it. Fixing that, and fixing what the panel then found in the fix, is most
of the work here.

## State of the goal

| Item | State |
|---|---|
| Sight gate (require_sight / required_sources) | DONE, live-verified through the daemon |
| agy locked out of the sight gate | FIXED. agy now dispatches `--output-format stream-json` |
| codex source-gating | DONE. Its parser classifies content readers |
| Write containment for reviews | DONE for agy and codex, proven by a DENIED WRITE, mutation-verified |
| Mandatory review is a rubber stamp | FIXED. Dispatches a real reviewer; REJECT and Indeterminate block |
| Chorus fix list FIND-GROK-01..05 | 02/03/05 done, 01 partial, 04 open |
| `GROK_CHORUS_NEXT.md` Goals 1.1, 1.2, 1.3, 1.5, 2, 3, 4, 5 | NOT STARTED |

## Next action, in order

`GROK_CHORUS_NEXT.md` is the assignment. Its suggested order:

1. **Goal 1.1** `review_submit` can still approve an unfinished row. `submit_review` in
   `crates/peer-review/src/lib.rs` has NO state guard: its UPDATE sets `state='done'` regardless
   of current state, so a `pending` row can be approved. Fix: require `in_progress` for an
   approve verdict, and check the submitter is the assigned reviewer. THIS IS WHERE I STOPPED.
2. Goal 1.3 (forge path test on the production route) and 1.2 (inflight cap).
3. Goal 1.5 (sight-gate the reviewer) and Goal 2 (Fast grok child, per-Command env, NOT a
   thread-local, `with_forced_fast` was deleted as unsound and must not return).
4. Goal 3 (>200 tool tripwire), Goal 5 (progress doc header), Goal 4 (fixtures) last.

## What physically exists now

- `bash scripts/verify-live-agents.sh [agy|codex|grok|review]` runs every opt-in guard.
  `review` runs the mandatory-review end-to-end tests with a MOCK reviewer, no network.
- `daemon/docs/grok-compat-claude.example.toml`, the expected `[compat.claude]` block.
- `~/.claude/scripts/peer-review/` (dispatch.sh, receipt.sh, BRIEF-TEMPLATE.md), the
  cross-project reviewer harness.
- `daemon/docs/specs/GOAL-sight-gate-progress.md`, the full round log including every wrong
  claim I made and who corrected it.

## Findings that contradict earlier documents in this same session

- I marked FIND-GROK-04 and FIND-GROK-05 **closed** and they were not. Grok caught both.
- `AGENTS_WITH_NO_WRITE_CONTAINMENT` was empty for the WRONG reason: the comment claimed
  `codex exec` is read-only, while Triumvirate launches codex
  `--dangerously-bypass-approvals-and-sandbox` by default.
- My peer-review harness carried the identical false claim, proven when an unsandboxed reviewer
  modified 14 files mid-review. `dispatch.sh` now passes `--sandbox read-only`.

## Operating constraints learned

- **Freeze the artifact before dispatching reviewers.** Editing mid-review destroyed two full
  Grok reviews.
- **One background task per peer.** Fanning out with `&` inside one wrapper kills the others
  when the first finishes.
- A reviewer returning zero tool calls may be YOUR harness: check permissions before blaming it.
- Two commits in history (`0a1be32`, `8c16f73`) were made BY A TEST. Left as evidence; the
  `set_current_dir` cause is unfixed.

## NOT verified

- Goal 1 items above are unstarted, not merely untested.
- `with_forced_fast` was removed; nothing currently isolates a grok panel child to Fast.
- Live tool captures for `run_terminal_command`, `search_replace`, `grep` do not exist.
