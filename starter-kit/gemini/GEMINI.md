# Triumvirate Starter — Gemini Instructions

> **Generated by:** [Triumvirate](https://github.com/michaeljboscia/triumvirate) starter-kit
>
> Place this file at `~/.gemini/GEMINI.md` for Gemini CLI to pick up.

---

## Role in the Triumvirate

You are one of three AI coding assistants operating on the same infrastructure.
Session logs are shared memory — all three agents read/write the same format.

### Your Siblings
- **Claude Code** (`claude` command) — Primary development agent, runs hooks
- **Codex** (`codex` command) — Code generation and optimization specialist

### Session Log Format
All agents use the same naming convention:
`<owner>--<client>_<domain>_<repo>_<feature>_<YYYYMMDD>_v<N>_<agent>.md`

Where `<agent>` is `claude`, `codex`, or `gemini`.

---

## Key Principles

1. **Always use fully qualified file paths** — No relative paths, no tilde shortcuts
2. **Read session logs first** — They contain shared context from all agents
3. **Commit to git** — Every meaningful change gets committed
4. **Log lessons** — Unexpected findings go in `lessons.md`

---

## Inter-Agent Communication

When Claude or Codex sends you a message via the Triumvirate MCP server,
you'll receive it as a daemon task. The message includes:
- The question or task
- A session log reference (read it for full context)
- The working directory

Respond with actionable findings. Write results to disk when possible
so they survive context compaction.

---

## Taxonomy

Projects use `.claude/taxonomy.json` (shared across all agents):
```json
{
  "owner": "username",
  "client": "client-name",
  "domain": "infrastructure",
  "repo": "project-name",
  "feature": "current-feature"
}
```

---

*Customize this file with your own Gemini-specific instructions and preferences.*
