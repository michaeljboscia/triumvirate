---
description: Query and manage the persistent Gemini oracle daemon for the current project
---

Read `~/.claude/skills/pythia/SKILL.md` for the complete Pythia Oracle Engine skill workflow, then follow its instructions exactly. If the skill file doesn't exist, check the triumvirate repo's `starter-kit/claude/skills/` directory.

The user's input (if any) after `/pythia` is the subcommand and arguments. If no subcommand given, show oracle status.
