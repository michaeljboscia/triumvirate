[package]
name = "mcp-bridge"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
agent-adapter = { path = "../agent-adapter" }
shared-types = { path = "../shared-types" }
